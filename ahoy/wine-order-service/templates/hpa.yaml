{{- range $key, $value := .Values.deployments }}
  {{- if $value.hpa }}
    {{- if $value.hpa.enabled }}
---
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ template "wine-order-service.fullname" $ }}-{{ $key }}
  labels:
    app: {{ template "wine-order-service.name" $ }}-{{ $key }}
    chart: {{ template "wine-order-service.chart" $ }}
    release: {{ $.Release.Name }}
    heritage: {{ $.Release.Service }}
    tribe: {{ $.Values.tribe }}
    squad: {{ $.Values.squad }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ template "wine-order-service.fullname" $ }}-{{ $key }}
  minReplicas: {{ $value.hpa.replicas.min }}
  maxReplicas: {{ $value.hpa.replicas.max }}
  metrics:
{{ toYaml $value.hpa.metrics | indent 2 }}
    {{- end }}
  {{- end }}
{{- end }}